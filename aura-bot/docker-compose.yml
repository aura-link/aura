services:
  aura-bot:
    build: .
    container_name: aura-bot
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data
    networks:
      - default
    extra_hosts:
      - "host.docker.internal:host-gateway"
